\documentclass[../slides]{subfiles}

\begin{document}
    \begin{tikzpicture}[level distance=2cm]
        \node[visible on=<-20>, xshift=-1cm] (root) {\(\mathit{S} \to [x_1\,\text{the}\,y_1\,x_2]\begin{bmatrix}\rootclause{}\end{bmatrix} (V_2, N)\)}
        [sibling distance=14em]
        child[visible on=<-20>] { node[visible on=<-18>] (vp1) {\(V_2 \to [x_1, \text{was}\, x_2] \begin{bmatrix}\uppervclause{}\end{bmatrix} (V_2)\)}
            [sibling distance=6.5em]
            child[visible on=<-18>] { node[visible on=<-17>] (vp2) {\(V_2 \to [x_1, \text{carried}\, y_1] \begin{bmatrix}\lowervclause{}\end{bmatrix} (\mathrm{arg}(V), V\binsym{})\)}
                child[visible on=<-17>] { node[visible on=<-15>] (term0) {\(\mathrm{arg}(V) \to [\text{where}] \begin{bmatrix}\whclause{}\end{bmatrix}\)}}
                child[visible on=<-17>] { node[visible on=<-16>, xshift=6em] (term5) {\(V\binsym{} \to [\text{out}] \begin{bmatrix}\prtclause{}\end{bmatrix}\)}}}}
        child[visible on=<-20>] { node[visible on=<-19>] (np) {\(N \to [\text{survey}] \begin{bmatrix}\nnclause{}\end{bmatrix}\)}};

        \begin{scope}
            \node[visible on=<21-|handout:0>] at (root) {\(\mathit{S} \to \begin{bmatrix}\rooteval{}\end{bmatrix}\)};
            \node[visible on=<19-20|handout:0>] at (vp1) {\(V_2 \to \begin{bmatrix}\upperveval{}\end{bmatrix}\)};
            \node[visible on=<18-18|handout:0>] at (vp2) {\(V_2 \to \begin{bmatrix}\lowerveval{}\end{bmatrix}\)};
            \node[visible on=<16-17|handout:0>] at (term0) {\(\mathrm{arg}(V) \to \begin{bmatrix}\wheval{}\end{bmatrix}\)};
            \node[visible on=<17-17|handout:0>] at (term5) {\(V\binsym{} \to \begin{bmatrix}\prteval{}\end{bmatrix}\)};
            \node[visible on=<20-20|handout:0>] at (np) {\(N \to \begin{bmatrix}\nneval{}\end{bmatrix}\)};
        \end{scope}
    \end{tikzpicture}
\end{document}