\documentclass[../slides]{subfiles}

\begin{document}%
    \tikzset{
        tcomp/.style={
            level distance=2ex,
            font=\scriptsize,
            inner sep=.4px,
            outer sep=.5px,
            sibling distance=1.2em,
            baseline={(current bounding box.center)},
            node distance=.2em,
            edge from parent path={(\tikzparentnode.south) -- ++(0,-.3ex) -| (\tikzchildnode.north)},
            every path/.style={line width=.15ex},
        },
    }%
    \newcommand{\rootclause}{\tikz[tcomp]\node{SBAR} child { node {S} child {node{$x$}} child { node {$y$} child { node {PT} child {node {2}}}}};}%
    \newcommand{\uppervclause}{\tikz[tcomp]\node{VP} child {node{$x$}} child { node {VBD} child { node {4}}};}%
    \newcommand{\lowervclause}{\tikz[tcomp]\node{VP} child {node{$x$}} child { node {$y$} child { node {VBN} child { node {5}}}};}%
    \newcommand{\whclause}{\tikz[tcomp]\node{WH} child {node{WRB} child {node {1}}};}%
    \newcommand{\prtclause}{\tikz[tcomp]{\node(r) {$a^\downarrow$};\node[right=of r]{PRT} child {node{RP} child {node {6}}};}}%
    \newcommand{\nnclause}{\tikz[tcomp]\node{NP} child {node {$a^\downarrow$}} child {node{NN} child {node {3}}};}%
    %
    \begin{tikzpicture}[anchor=center, outer sep=0px, inner sep=1pt]
        \node (root) {\(\mathit{Root} \to [x_1\,\text{2}\,y_1\,x_2]\begin{bmatrix}\rootclause{}\end{bmatrix} (V_2, N)\)}
        [sibling distance=14em]
        child { node (vp1) {\(V_2 \to [x_1, \text{4}\, x_2] \begin{bmatrix}\uppervclause{}\end{bmatrix} (V_2)\)}
            [sibling distance=6.5em]
            child{ node (vp2) {\(V_2 \to [x_1, \text{5}\, y_1] \begin{bmatrix}\lowervclause{}\end{bmatrix} (V^L, V^R)\)}
                child { node (term0) {\(V^L \to[\text{1}] \begin{bmatrix}\whclause{}\end{bmatrix}\)}}
                child { node[xshift=3em] (term5) {\(V^R \to [\text{6}] \begin{bmatrix}\prtclause{}\end{bmatrix}\)}}}}
        child { node (np) {\(N \to [\text{3}] \begin{bmatrix}\nnclause{}\end{bmatrix}\)}};
    \end{tikzpicture}
\end{document}